# あそびばこ

4〜6歳のこども向けの、ミニゲームがたくさん詰まった知育アプリです。オリジナルキャラクターの
**くまた・みけ・ぴょん**が案内役として登場します。
**外出先でオフラインで遊ぶこと**を前提に、**費用0円**で運用できるように設計しています。
既存のコードは全面的に作り直し、対象年齢・遊び方・技術構成をゼロから見直しました。

## キャラクターについて

アンパンマンやハローキティ、スヌーピーのような有名キャラクターは、それぞれの著作権者(やなせたかし/フレーベル館など、サンリオ、Peanuts/Charles M. Schulz)が権利を持つため、似せて描いて公開すると著作権・商標の侵害になります。無料・非商用のアプリであっても使用できません。
そのため本アプリでは、**このアプリのためだけに描き起こしたオリジナルキャラクター3体**(`tools/generate-characters.js`でコード生成、外部素材0点)を採用しています。

| キャラクター | 名前 | 登場するゲーム |
|---|---|---|
| 🐻 くま | くまた | どうぶつクイズ の案内役 |
| 🐱 ねこ | みけ | えあわせ のカード、なでなで |
| 🐰 うさぎ | ぴょん | かずあそび の案内役 |
| 🐶 いぬ | ポチ | なでなで |

## 設計方針(なぜこう作ったか)

4〜6歳児が一人で(または親のスマホを借りて)外出先で使うことを想定し、以下を徹底しています。

- **「失敗」で終わらない**: ゲームオーバーやライフ制はありません。クイズ系ゲームでは間違えても優しく揺れて再挑戦できるだけで、正解するまで何度でも挑戦できます。
- **少し考える・見分ける要素がある**: 単純な連打だけでなく、「同じ絵をさがす」「数を数える」「鳴き声から動物をあてる」「似た色を見分ける」など、4〜6歳の発達に合わせた軽い難易度を用意しています。
- **文字が読めなくても遊べる**: ボタンは絵文字・キャラクター・色で判断できます。音声読み上げ(SpeechSynthesis)が案内してくれます。
- **指1本〜マルチタッチ対応、的が大きい**: タップ対象は最低52px以上を確保。
- **誤操作でアプリを閉じない**: ホームに戻るボタンは0.65秒**長押し**しないと発動しません(連打しても外に出ません)。
- **縦・横どちらでも遊べる**: 外出先ではスマホを縦に持つことが多いため、回転を強制しません。
- **音声・効果音は合成音**: 音声ファイルを一切使わず、Web Audio APIとブラウザ標準の音声合成(SpeechSynthesis)でその場生成しています。アプリの容量が増えず、著作権や素材コストもかかりません。
- **オフライン動作**: Service Workerで全ページを端末にキャッシュするので、電波が無い場所でも遊べます。
- **広告・課金・通信なし**: 外部通信や個人情報の送信は一切ありません(保護者の方も安心して渡せます)。

## あそび一覧(13種類)

### まずは慣れる、失敗のないあそび

| ゲーム | 内容 |
|---|---|
| 🫧 しゃぼんだま | 浮かんでくる泡を指でポンポン割る |
| 🎨 おえかき | 指でなぞるとレインボーの線が描ける、指ごとに違う色 |
| 🐶 なきごえ | 動物をタップすると鳴き声(擬音)を発音してくれる |
| 🎆 はなび | 画面をタップした場所に花火が上がる(マルチタッチ対応) |
| 💧 あめふり | 画面をタップすると波紋が広がる、たまに生き物が顔を出す |

### 少し考える・見分けるあそび(4〜6歳向けに難易度アップ)

| ゲーム | 内容 |
|---|---|
| ⭐ ほしとり | 降ってくる星をタップしてキャッチ、瓶にたまっていく(速め設定) |
| 🧩 かたちあわせ | 図形のピース(まる・しかく・さんかく・ほし・ろっかく・はーと の6種)を同じ形の枠までドラッグ |
| 🌈 いろさがし | 「あか はどこ?」と聞かれた色を8色の中からタップ(似た色も混ざります) |
| 🐾 どうぶつクイズ | くまたが鳴き声を教えてくれるので、正しい動物を4択から選ぶ |
| 🃏 えあわせ | カードをめくって同じ絵のペアを見つける神経衰弱(6ペア/12枚) |
| 🔢 かずあそび | 画面に散らばった物の数をかぞえて、正しい数字をタップ(1〜10) |
| 🐾 なでなで | みけ(ねこ)・ポチ(いぬ)をなでたり、ごはんをあげたりしてふれあう |
| 🛗 エレベーター | 階数ボタンを押すと数字が光り、階数表示が動いて、ポーンと鳴って扉が開く |

## 保護者の方へ

- 広告・アプリ内課金・外部通信は一切ありません。
- 遊んでいる最中に個人情報を入力する場面はありません。
- 音を消したいときは画面右上の 🔊 ボタンをタップしてください。
- ゲームをやめて選択画面に戻るには、画面左上の 🏠 ボタンを**少し長押し**してください(誤タップ防止のためです)。

## 公開方法(費用: 0円)

このアプリはビルド不要のプレーンな HTML/CSS/JS のみで構成されているため、**GitHub Pages に無料でそのまま公開**できます。

1. このリポジトリを GitHub にプッシュします。
2. リポジトリの **Settings > Pages** を開きます。
3. **Source** で `Deploy from a branch` を選び、公開したいブランチと `/ (root)` を選択して **Save** します。
4. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` で公開されます。

パスはすべて相対パスで書かれているため、ドメイン直下でもサブディレクトリ(GitHub Pagesのプロジェクトページ)でもそのまま動作します。

## Android への導入(費用: 0円)

### 方法A: ホーム画面に追加する(PWA、推奨・完全無料)

Google Play への登録は不要です。

1. Android の Chrome で公開した URL を開きます。
2. メニュー(⋮)から「ホーム画面に追加」または「アプリをインストール」をタップします。
3. ホーム画面のアイコンから起動すると、アドレスバーの無い全画面アプリとして動作します。
4. 一度開けば Service Worker が内容をキャッシュするので、以降は**電波が無くても**遊べます。

### 方法B: Google Play にアプリとして公開したい場合(任意)

無料の [PWABuilder](https://www.pwabuilder.com/)(Microsoft製)にサイトの URL を入力すると、このPWAをラップした Android パッケージ(AAB/APK)を無料で生成できます。生成したAPKは**サイドローディング(直接インストール)なら完全無料**です。Google Play ストアに掲載したい場合のみ、Google Play Console の登録料(一度きり US$25)が別途かかります。外出先での利用が目的であれば方法Aだけで十分です。

## 開発者向け情報

### ローカルで動かす

```bash
python3 -m http.server 8000
# http://localhost:8000/ にアクセス
```

ビルドツール・npm install は不要です(依存パッケージ0個)。

### ディレクトリ構成

```
index.html          # ホーム画面(ゲーム選択)
manifest.json        # PWAマニフェスト
sw.js                 # オフラインキャッシュ用 Service Worker
icons/                # アプリアイコン(コード生成、tools/generate-icons.js参照)
  characters/          # オリジナルマスコット3体のPNG(tools/generate-characters.js参照)
shared/
  style.css          # 共通デザインシステム(4〜6歳向けUIルール)
  app.js             # 共通ロジック(効果音合成/音声読み上げ/紙吹雪/ホームボタン/マスコット吹き出し/SW登録)
games/
  <game-name>/
    index.html
    script.js
tools/
  lib/pixel-canvas.js    # 外部ライブラリ無しのPNG描画エンジン(全生成スクリプトが共有)
  generate-icons.js      # アプリアイコンを生成
  generate-characters.js # マスコット3体を生成
```

### 新しいゲームの追加方法

1. `games/<slug>/` フォルダを作り、`index.html` と `script.js` を追加します。
2. `<head>` で `../../shared/style.css` を読み込み、`<body>` の最後で `../../shared/app.js` → 自分の `script.js` の順に読み込みます。
3. `script.js` の先頭で `KidsApp.initCommon();` を呼ぶと、ホームボタン・ミュートボタン・Service Worker登録が自動で入ります。
4. 効果音は `KidsApp.Sound.pop()/chime()/success()/boing()/splash()/whoosh()/tap()`、読み上げは `KidsApp.speak('わんわん')` が使えます(いずれも追加ファイル不要)。
5. マスコットを案内役に使う場合は `KidsApp.CHARACTERS`(くまた/みけ/ぴょん)から選び、`KidsApp.mascotBubble(container, {img, name, text})` で吹き出しを表示できます。
6. `index.html`(ホーム画面)の `.game-grid` にタイルを1つ追加します。
7. `sw.js` の `PRECACHE_URLS` に新しいファイルパスを追加し、`CACHE_VERSION` を1つ上げると、オフラインでも新しいゲームが遊べるようになります(バージョンを上げないと既存ユーザーの端末にキャッシュされた古い一覧のままになります)。

### 設計ルール(新しいゲームを作るときのチェックリスト)

- [ ] ゲームオーバーや後戻りできない失敗を作らない(間違えても穏やかに再挑戦できる)
- [ ] タップ/ドラッグ対象は指より大きく(目安52px以上)
- [ ] 文字を読まなくても遊び方がわかる(音声・絵・色でヒントを出す)
- [ ] 縦・横どちらの向きでも崩れない
- [ ] キャラクターを使う場合は `icons/characters/` のオリジナルキャラクターのみ使用する(実在の著作権付きキャラクターを模写・使用しない)
